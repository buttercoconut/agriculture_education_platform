# README for backend
# This is a minimal FastAPI backend for the Agriculture Education Platform.
# It includes user authentication, course CRUD, and community post endpoints.
# For production use, replace in-memory stores with a real PostgreSQL database.
# Run with: uvicorn backend.main:app --reload
